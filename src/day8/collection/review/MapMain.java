package day8.collection.review;

import java.util.HashMap;
import java.util.Map;

public class MapMain {

    public static void main(String[] args) {
        // Đi vào mall, cần gửi đồ
        // Ngta đưa cho mình cái chìa khoá, và
        // chỉ mở được đúng cái tủ đồ
        // Key ko được phép trùng
        // nếu mà trùng key ghi đè dữ liệu(value)
        // ko theo order/ thứ tự

        // Map<K,V> -> K: Key, V: Value
        Map<String, String> map = new HashMap<>();

        // thay vì là add -> put(key, value)
        // remove thì remove theo key
    }
}
